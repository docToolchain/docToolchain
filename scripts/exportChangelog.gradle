//tag::exportChangeLog[]
task exportChangeLog(
        description: 'exports the change log from a git subpath',
        group: 'docToolchain'
) doLast {
    // https://git-scm.com/docs/pretty-formats
    def changes = "git log --pretty=format:\"| %ad %n| %an %n| %s %n\" --date=short ${srcDir}/arc42".execute().text
    new File(targetDir).mkdirs()
    def changelog = new File(targetDir, 'changelog.adoc')
    changelog.write(changes)
}
//end::exportChangeLog[]
